@model List<Movie>
<partial name="_NotificationPartial" />

<div class="card shadow">

    <div class="card-header bg-dark bg-gradient">
        <div class="row">
            <div class="col-12 text-center">
                <h2>Movie List</h2>
            </div>
        </div>
    </div>
    
    <div data-bs-theme="dark" class="card-body mb-5 bg-gradient">

        <div class="row pb-3">
            <div class="col-12 text-end">
                <a asp-area="Admin" asp-controller="Movie" asp-action="Add" class="btn btn-info">
                    <i class="bi bi-plus-circle"></i> Add Movie
                </a>
            </div>
        </div>

        <table id="tableData" class="table table-responsive table-striped table-hover bg-gradient table-primary table-bordered" style="width:100%">
            <thead class="text-center">
                <tr class="table-primary">
                    <th>Title</th>
                    <th>Director</th>
                    <th>Year Released</th>
                    <th class="text-center">Edit / Delete</th>
                </tr>
                </thead>
            @*<tbody class="table-primary">
            @foreach (var movie in Model.OrderBy(m => m.Title))
            {
                        <tr class="text-center">
                            <td>@movie.Title</td>
                            <td>@String.Format($"{movie.Director.Substring(movie.Director.IndexOf(" ")+1)}, {movie.Director.Substring(0,movie.Director.IndexOf(" "))}")</td>
                            <td>@movie.YearReleased</td>
                            <td>
                                <div class="btn-group w-100" role="group">
                                    <a asp-area="Admin" asp-controller="Movie" asp-action="Edit" asp-route-id="@movie.Id" class="btn btn-secondary mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit
                                    </a>
                                <a onClick="confirmDelete('/admin/movie/delete/@movie.Id')" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash3-fill"></i> Delete
                                    </a>
                                </div>
                            </td>
                        </tr>
            }
                </tbody>*@
        </table>

    </div>

</div>


@section Scripts{
    <script src="~/js/movie.js"></script>
}
